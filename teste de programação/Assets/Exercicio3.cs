using UnityEngine;

public class Exercicio3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int pontos = 10;
        int mult = 3;
        int total = pontos * mult;
        print("total: " + total);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
